{% extends "Templates/default.twig" %}

{% block body %}

	<ul>
		{% for kind in kinds %}
			<li>
				<a href="{{ url("postBlocks.create", { postId: post.getId, kindCode: kind.getCode }) }}">
					{{ kind.getTitle }}
				</a>
			</li>
		{% endfor %}
	</ul>

{% endblock body %}
