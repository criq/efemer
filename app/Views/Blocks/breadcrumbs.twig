<nav>
	<ul class="a-breadcrumbs">
		{% for breadcrumb in breadcrumbs %}
			<li>
				{% if breadcrumb.getURL %}
					<a href="{{ breadcrumb.getURL }}">
				{% endif %}
				{{ breadcrumb.getTitle }}
				{% if breadcrumb.getURL %}
					</a>
				{% endif %}
			</li>
		{% endfor %}
	</ul>
</nav>
